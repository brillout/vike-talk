import { CodeBlock } from '../../components'

# First-class SSR/SPA/SSG

<p style={{fontSize: '1em', marginBottom: 24, marginTop: -8}}>
  Toggle{' '}
  <span style={{background:'#b8d4eb', borderRadius: 4, padding: '1px 5px'}}>SSR</span>/<span style={{background:'#c0ebb8', borderRadius: 4, padding: '1px 5px'}}>SPA</span>/<span style={{background:'#e9b8e8', borderRadius: 4, padding: '1px 5px'}}>SSG</span>
  {' '}on a page-by-page basis, powered by{' '}
  <span style={{background:'#f4f5a6', borderRadius: 4, padding: '1px 5px'}}>config inheritance</span>.
</p>

<div style={{display:'flex', gap: 0}}>
<CodeBlock style={{flex: 1.4, fontSize: 16}}>
```yaml
/pages/product/+config.js # [!code highlight:#f4f5a6]
# SSR pages // [!code highlight:#b8d4eb]
/pages/product/@id/+Page.js         # URL: /product/42 # [!code highlight:#b8d4eb]
/pages/product/@id/buy/+Page.js     # URL: /product/42/buy# [!code highlight:#b8d4eb]

/pages/admin/+config.js # [!code highlight:#f4f5a6]
# SPA pages (i.e. SSR disabled) // [!code highlight:#c0ebb8]
/pages/admin/products/+Page.js      # URL: /admin/products# [!code highlight:#c0ebb8]
/pages/admin/users/+Page.js         # URL: /admin/users # [!code highlight:#c0ebb8]

/pages/(marketing)/+config.js # [!code highlight:#f4f5a6]
# SSG pages (i.e. pre-rendering) // [!code highlight:#e9b8e8]
/pages/(marketing)/index/+Page.js   # URL: / # [!code highlight:#e9b8e8]
/pages/(marketing)/about/+Page.js   # URL: /about # [!code highlight:#e9b8e8]
```
</CodeBlock>

<div style={{display:'flex', flexDirection:'column', gap: 16, flex: 1.5}}>
<CodeBlock style={{fontSize: 16}}>
```js
// /pages/product/+config.js // [!code highlight:#f4f5a6]
// » config for product pages: /pages/product/**/+Page.js // [!code highlight:#f4f5a6]
export default {
  ssr: true // [!code highlight:#b8d4eb]
}
```
</CodeBlock>
<CodeBlock style={{fontSize: 16}}>
```js
// /pages/admin/+config.js // [!code highlight:#f4f5a6]
// » config for admin pages: /pages/admin/**/+Page.js // [!code highlight:#f4f5a6]
export default {
  ssr: false // => SPA // [!code highlight:#c0ebb8]
}
```
</CodeBlock>
<CodeBlock style={{fontSize: 16}}>
```js
// /pages/(marketing)/+config.js // [!code highlight:#f4f5a6]
// » config for marketing pages: /pages/(marketing)/**/+Page.js // [!code highlight:#f4f5a6]
export default {
  prerender: true // aka SSG // [!code highlight:#e9b8e8]
}
```
</CodeBlock>
</div>
</div>
