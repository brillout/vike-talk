import { Hr } from '../../components'

## SSR

Use SSR if your website's content should appear in search engines and AI.

*Example*: landing page

## SPA

Con:
- Slower initial page load, especially on mobile devices

Pro:
- No production server (SSG isn't always an option)
- Only client-side code (easier to write)

*Example*: admin panel
