# Architecture

- Agnostic foundation
- Composed of stable primitives
  <div style={{opacity: 0.5, fontSize: '0.8em', marginTop: -5}}>
  Client-side routing, URL handling,  
  Pre-rendering, HTML streaming,  
  Filesystem routing, Data fetching, Hooks,  
  Config inheritance, Layouts, Error handling,  
  `pageContext` & `globalContext`,  
  CSP,  Layouts, Route Guards,  
  ...
  </div>
